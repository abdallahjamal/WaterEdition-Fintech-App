package com.abood.wateredition.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand ──────────────────────────────────────────────────────────────────
val CyanPrimary      = Color(0xFF00D4FF)  // Electric Cyan — accent
val CyanLight        = Color(0xFF64E9FF)
val CyanDark         = Color(0xFF0099CC)

// ── Backgrounds ─────────────────────────────────────────────────────────────
val NavyBackground   = Color(0xFF0A0E1A)  // Deep Navy — app background
val NavySurface      = Color(0xFF111827)  // Dark surface
val NavyCard         = Color(0xFF1A2235)  // Card / elevated surface
val NavyCardBorder   = Color(0xFF243048)  // Subtle card border

// ── Semantic ────────────────────────────────────────────────────────────────
val DebtRed          = Color(0xFFFF4D6D)  // Balance > 0  → debt
val DebtRedMuted     = Color(0xFF3D1220)  // Debt container background
val CreditGreen      = Color(0xFF00C48C)  // Balance < 0  → credit
val CreditGreenMuted = Color(0xFF062E22)  // Credit container background
val NeutralGrey      = Color(0xFF9CA3AF)  // Balance = 0  → settled
val NeutralGreyMuted = Color(0xFF1F2937)  // Settled container background

// ── Text ────────────────────────────────────────────────────────────────────
val TextPrimary      = Color(0xFFF9FAFB)
val TextSecondary    = Color(0xFF9CA3AF)
val TextDisabled     = Color(0xFF4B5563)

// ── Misc ─────────────────────────────────────────────────────────────────────
val DividerColor     = Color(0xFF1F2937)
val FillBlue         = Color(0xFF3B82F6)   // Fill transaction icon color
val PaymentGold      = Color(0xFFF59E0B)   // Payment transaction icon color